// dependencies
import styled from "styled-components";

const AdminHeadline = styled.h1`
  color: ${({ theme: t }) => t.textPrimary};
  font-size: 4rem;
  font-weight: 700;
`;

const Otherthing = styled.span``;

export { AdminHeadline, Otherthing };
